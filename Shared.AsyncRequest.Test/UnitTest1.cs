using FluentAssertions;
using Shared.AsyncRequest.Extensions;

namespace Shared.AsyncRequest.Test
{
    public class UnitTest1
    {
        private HttpRequestMessage _request;
        
        //[Fact]
        //public void GetArguments_Of_GetRequestArgumentDescriptor_Should_Return_QueryStrings()
        //{
        //    // arrange
        //    const int somePageSize = 10;
        //    var someUri = new Uri($"http://{Guid.NewGuid()}.ir?pageSize={somePageSize}");

        //    _request = new HttpRequestMessage(HttpMethod.Get, someUri);

        //    // act
        //    var actual = _request.GetArguments();

        //    // assert
        //    actual
        //        .Should()
        //        .ContainEquivalentOf(somePageSize);
        //}
    }
}